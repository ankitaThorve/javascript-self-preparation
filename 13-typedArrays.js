// Typed Arrays ----------------
// Typed arrays was designed for handling Binary Data.Unlike arrays, typed arrays are buffers of Fixed Length.Typed arrays store elements of Fixed Types like 8-bit integers or 32-bit numbers.

const myArr = new Uint8Array(5);
console.log(`Array: ${myArr} \nBytes per element: ${myArr.BYTES_PER_ELEMENT}`);
